
def function_name(param1: type, param2: type) -> return_type:
    """Sort description of the function

    Args:
        param1: Explain the first parameter
        param2: Explain the second parameter.
    Returns:
        Explain what the function returns
    """
    pass

"""the java way of doing a function

public static functionName(int a, int b) {

} 
"""

# sum
def sum(numbers: List[float]) -> float:
    """Adds up all numbers in a list.

    Args:
        numbers: A list of numbers
    Returns:
        The sum of the numbers in a list.
    """
    pass

# math.ceil
def ceil(num: float) -> int:
    """Rounds a decimal number up to the nearest greater interger

    Args:
        num: A decimal number
    Returns:
        The number rounded up.
    """
    pass

# math.floor

# Function that takes a name and outputs a message
def create_message(name: str) -> str:
    """Returns a personalized message

    Args:
        name: person's name
    Returns:
        Pesonalized message including someone's name.
    """
    pass

# Function that takes two floats and finds their average
def avg(a: float, b: float) -> float:
    """Gets the average of two numbers.

    Args:
        a: A number
        b: Another number
    Returns:
        The average of the two numbers.
    """
    pass

# Function that takes an age and returns True if they are of voting ages
def can_vote(age: int) -> bool
    """Determines whether or not a person can vote

    Args:
        age: someone's age
    Return:
        True if they can vote, False if they can't
    """
    pass